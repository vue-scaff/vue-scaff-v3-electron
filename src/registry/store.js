export default ({}) => {
  const state = {
    alive: ['start'],
    oss: `https://oss.vue-scaff.com`,
  };

  const mutations = {};

  const actions = {};

  return { state, mutations, actions };
};
